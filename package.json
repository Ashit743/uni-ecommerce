{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  },
  "scripts":{
    "client-uniform": "cd packages/package-uniform && npm run dev",
    "client-uniform-test": "cd packages/package-uniform && npm run test"
  }
}
